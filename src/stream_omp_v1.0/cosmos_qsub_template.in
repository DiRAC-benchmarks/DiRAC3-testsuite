#!/bin/bash


#PBS -l nodes=${STREAM_NODES}:ppn=${SYSTEM_CORES_PER_NODE}
#PBS -l walltime=00:30:00
#PBS -A dr002
#PBS -m bea

source /etc/profile.d/modules.sh


pushd ${DIRAC3_TESTSUITE_OUTPUTS_PATH} 

export OMP_NUM_THREADS=${STREAM_OMP_NUM_THREADS}

${DIRAC3_TESTSUITE_BIN_PATH}/stream_omp_v1

popd
