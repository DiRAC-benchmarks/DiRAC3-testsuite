pushd ${DIRAC3_TESTSUITE_RUN_PATH}
ln -s ${CL3D_INPUT_FILE}

# export KMP_AFFINITY=verbose,scatter

echo ${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 1 ${MPIEXEC_PREFLAGS} $<TARGET_FILE:clover_leaf>
${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} 1 ${MPIEXEC_PREFLAGS} $<TARGET_FILE:clover_leaf>

popd
