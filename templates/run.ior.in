#!/usr/bin/env bash
pushd ${DIRAC3_TESTSUITE_RUN_PATH}

blockSize=1g
${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} ${DIRAC3_JOB_NODES} ${MPIEXEC_PREFLAGS} $<TARGET_FILE:ior> -vvv -b $blockSize -f ${IOR_INPUT_FILE} 1> ${DIRAC3_JOB_OUTFILE} 2> ${DIRAC3_JOB_ERRFILE}

popd
