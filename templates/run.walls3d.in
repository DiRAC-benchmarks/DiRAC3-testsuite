pushd ${DIRAC3_TESTSUITE_RUN_PATH}
ln -s ${WALLS_INPUT_FILE}

export OMP_NUM_THREADS=${HOST_CPUS_PER_NODE}
dd=512

${MPIEXEC} ${MPIEXEC_NUMPROC_FLAG} ${DIRAC3_JOB_NODES} ${MPIEXEC_PREFLAGS} ${EXECUTABLE_OUTPUT_PATH}/walls_xeon_mpi_3d $dd $dd $dd ${WALLS_MPI_LAYOUT} $dd $dd $dd

popd
