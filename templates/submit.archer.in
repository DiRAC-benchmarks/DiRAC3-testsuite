#!/bin/bash --login

#PBS -A d63
#PBS -q short
#PBS -l select=1
#PBS -N ${JOB_NAME}
#PBS -l walltime=00:10:00

# Load modules
source ${SCRIPT_MODULES}

# OpenMP threads
echo export OMP_NUM_THREADS=24
export OMP_NUM_THREADS=24

# Run the benchmark
source ${SCRIPT_RUN}
