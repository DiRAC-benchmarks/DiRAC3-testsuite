#!/bin/bash --login

#PBS -l nodes=1
#PBS -N ${JOB_NAME}
#PBS -l walltime=00:10:00

# Load modules
source ${SCRIPT_MODULES}

# OpenMP threads
echo export OMP_NUM_THREADS=1
export OMP_NUM_THREADS=1

# Run the benchmark
source ${SCRIPT_RUN}
